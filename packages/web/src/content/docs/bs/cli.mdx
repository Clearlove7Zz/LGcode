---
title: CLI
description: Loongcode CLI opcije i naredbe.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Loongcode CLI po defaultu pokreće [TUI](/docs/tui) kada se pokrene bez ikakvih argumenata.

```bash
loongcode
```

Ali takođe prihvata naredbe kao što je dokumentovano na ovoj stranici. Ovo vam omogućava programsku interakciju sa Loongcode.

```bash
loongcode run "Explain how closures work in JavaScript"
```

---

### tui

Pokrenite Loongcode terminalski korisnički interfejs.

```bash
loongcode [project]
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                     |
| ---------------------------------------- | ------ | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                              |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                    |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |        | Prompt za upotrebu                                                       |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za korištenje u obliku provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za korištenje                                                      |
| <nobr><code>{"--port"}</code></nobr>     |        | Port na kojem treba slušati                                              |
| <nobr><code>{"--hostname"}</code></nobr> |        | Hostname na kojem treba slušati                                          |

---

## Naredbe

Loongcode CLI takođe ima sljedeće naredbe.

---

### agent

Upravljajte Loongcode agentima.

```bash
loongcode agent [command]
```

---

### attach

Priključite terminal na već pokrenut Loongcode backend server pokrenut putem `serve` ili `web` naredbi.

```bash
loongcode attach [url]
```

Ovo omogućava korištenje TUI-ja sa udaljenim Loongcode backend-om. Na primjer:

```bash
# Start the backend server for web/mobile access
loongcode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
loongcode attach http://10.20.30.40:4096
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |        | Radni direktorij za pokretanje TUI-a                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavi posljednju sesiju                                                                     |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkuj sesiju prilikom nastavka (koristite sa `--continue` ili `--session`)                   |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `LOONGCODE_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | Korisničko ime za osnovnu autentifikaciju (zadano: `LOONGCODE_SERVER_USERNAME` ili `loongcode`) |

---

#### create

Kreirajte novog agenta s prilagođenom konfiguracijom.

```bash
loongcode agent create
```

Ova naredba će vas voditi kroz kreiranje novog agenta sa prilagođenim sistemskim promptom i konfiguracijom alata.

---

#### list

Navedite sve dostupne agente.

```bash
loongcode agent list
```

---

### auth

Naredba za upravljanje vjerodajnicama i prijavom za provajdere.

```bash
loongcode auth [command]
```

---

#### login

Loongcode pokreće lista provajdera na [Models.dev](https://models.dev), tako da možete koristiti `loongcode auth login` da konfigurirate API ključeve za bilo kojeg provajdera kojeg želite koristiti. Ovo je pohranjeno u `~/.local/share/loongcode/auth.json`.

```bash
loongcode auth login
```

Kada se Loongcode pokrene, učitava dobavljače iz datoteke vjerodajnica. I ako postoje neki ključevi definirani u vašim okruženjima ili `.env` fajl u vašem projektu.

---

#### list

Navodi sve autentifikovane dobavljače pohranjene u datoteci vjerodajnica.

```bash
loongcode auth list
```

Ili kratka verzija.

```bash
loongcode auth ls
```

---

#### logout

Odjavljuje vas s provajdera tako što ga briše iz datoteke vjerodajnica.

```bash
loongcode auth logout
```

---

### github

Upravljajte GitHub agentom za automatizaciju repozitorija.

```bash
loongcode github [command]
```

---

#### install

Instalirajte GitHub agenta u svoj repozitorij.

```bash
loongcode github install
```

Ovo postavlja neophodni tok rada GitHub Actions i vodi vas kroz proces konfiguracije. [Saznajte više](/docs/github).

---

#### run

Pokrenite GitHub agent. Ovo se obično koristi u GitHub Actions.

```bash
loongcode github run
```

##### Opcije

| Opcija                                | Opis                                   |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event za pokretanje agenta |
| <nobr><code>{"--token"}</code></nobr> | GitHub Personal Access Token           |

---

### mcp

Upravljajte Model Context Protocol (MCP) serverima.

```bash
loongcode mcp [command]
```

---

#### add

Dodajte MCP server svojoj konfiguraciji.

```bash
loongcode mcp add
```

Ova naredba će vas voditi kroz dodavanje lokalnog ili udaljenog MCP servera.

---

#### list

Navedite sve konfigurirane MCP servere i njihov status veze.

```bash
loongcode mcp list
```

Ili koristite kratku verziju.

```bash
loongcode mcp ls
```

---

#### auth

Autentifikujte se sa MCP serverom koji je omogućen za OAuth.

```bash
loongcode mcp auth [name]
```

Ako ne navedete ime servera, od vas će biti zatraženo da izaberete neki od dostupnih servera koji podržavaju OAuth.
Također možete navesti servere koji podržavaju OAuth i njihov status autentifikacije.

```bash
loongcode mcp auth list
```

Ili koristite kratku verziju.

```bash
loongcode mcp auth ls
```

---

#### logout

Uklonite OAuth vjerodajnice za MCP server.

```bash
loongcode mcp logout [name]
```

---

#### debug

Otklanjanje grešaka (debug) OAuth veze sa MCP serverom.

```bash
loongcode mcp debug <name>
```

---

### models

Navedite sve dostupne modele konfiguriranih provajdera.

```bash
loongcode models [provider]
```

Ova naredba prikazuje sve modele dostupne kod vaših konfiguriranih provajdera u formatu `provider/model`.
Ovo je korisno za pronalaženje tačnog naziva modela za korištenje u [vašoj konfiguraciji](/docs/config/).
Opciono možete proslijediti ID provajdera za filtriranje modela po tom dobavljaču.

```bash
loongcode models anthropic
```

#### Opcije

| Opcija                                  | Opis                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Osvježite keš modela sa models.dev                                       |
| <nobr><code>{"--verbose"}</code></nobr> | Koristite detaljniji izlaz modela (uključuje metapodatke poput troškova) |

Koristite `--refresh` zastavicu da ažurirate keširanu listu modela. Ovo je korisno kada su novi modeli dodani provajderu i želite da ih vidite u Loongcode.

```bash
loongcode models --refresh
```

---

### run

Pokrenite Loongcode u neinteraktivnom modu tako što ćete direktno proslijediti prompt.

```bash
loongcode run [message..]
```

Ovo je korisno za skriptiranje, automatizaciju ili kada želite brz odgovor bez pokretanja punog TUI-ja. Na primjer:

```bash "loongcode run"
loongcode run Explain the use of context in Go
```

Također možete priključiti pokrenutu `loongcode serve` instancu kako biste izbjegli vrijeme hladnog pokretanja MCP servera pri svakom pokretanju:

```bash
# Start a headless server in one terminal
loongcode serve

# In another terminal, run commands that attach to it
loongcode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |        | Naredba za pokretanje, koristite poruku za argumente                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`)                      |
| <nobr><code>{"--share"}</code></nobr>    |        | Podijelite sesiju                                                                             |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za korištenje u obliku provider/model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za korištenje                                                                           |
| <nobr><code>{"--file"}</code></nobr>     | `-f`   | Fajlovi koje treba priložiti poruci                                                           |
| <nobr><code>{"--format"}</code></nobr>   |        | Format: default (formatiran) ili json (sirovi JSON događaji)                                  |
| <nobr><code>{"--title"}</code></nobr>    |        | Naslov sesije (koristi skraćeni prompt ako nije navedena vrijednost)                          |
| <nobr><code>{"--attach"}</code></nobr>   |        | Priključite na pokrenuti Loongcode server (npr. http://localhost:4096)                         |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `LOONGCODE_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | Korisničko ime za osnovnu autentifikaciju (zadano: `LOONGCODE_SERVER_USERNAME` ili `loongcode`) |
| <nobr><code>{"--dir"}</code></nobr>      |        | Direktorij za pokretanje, ili putanja na udaljenom serveru pri spajanju                       |
| <nobr><code>{"--variant"}</code></nobr>  |        | Varijanta modela (napor zaključivanja specifičan za provajdera)                               |
| <nobr><code>{"--thinking"}</code></nobr> |        | Prikaži blokove razmišljanja                                                                  |
| <nobr><code>{"--port"}</code></nobr>     |        | Port za lokalni server (zadano na nasumični port)                                             |

---

### serve

Pokrenite Loongcode headless server za API pristup. Pogledajte [server docs](/docs/server) za kompletan HTTP interfejs.

```bash
loongcode serve
```

Ovo pokreće HTTP server koji pruža API pristup funkcionalnosti Loongcode-a bez TUI interfejsa. Postavite `LOONGCODE_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `loongcode`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba slušati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | Omogući mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### session

Upravljajte Loongcode sesijama.

```bash
loongcode session [command]
```

---

#### list

Navedite sve Loongcode sesije.

```bash
loongcode session list
```

##### Opcije

| Opcija                                    | Kratko | Opis                                   |
| ----------------------------------------- | ------ | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`   | Ograničenje na N najnovijih sesija     |
| <nobr><code>{"--format"}</code></nobr>    |        | Izlazni format: table ili json (table) |

---

### stats

Prikaži statistiku upotrebe tokena i troškova za vaše Loongcode sesije.

```bash
loongcode stats
```

#### Opcije

| Opcija                                  | Opis                                                                                                       |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Prikaži statistiku za zadnjih N dana (sva vremena)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Broj alata za prikaz (svi)                                                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Prikaži raščlambu korištenja modela (skriveno prema zadanim postavkama). Proslijedite broj za prikaz top N |
| <nobr><code>{"--project"}</code></nobr> | Filtriraj po projektu (svi projekti, prazan niz: trenutni projekt)                                         |

---

### export

Izvezite podatke sesije kao JSON.

```bash
loongcode export [sessionID]
```

Ako ne unesete ID sesije, od vas će biti zatraženo da odaberete neku od dostupnih sesija.

---

### import

Uvezite podatke sesije iz JSON datoteke ili Loongcode dijeljenog URL-a.

```bash
loongcode import <file>
```

Možete uvesti iz lokalne datoteke ili Loongcode dijeljenog URL-a.

```bash
loongcode import session.json
loongcode import https://opncd.ai/s/abc123
```

---

### web

Pokrenite Loongcode headless server sa web interfejsom.

```bash
loongcode web
```

Ovo pokreće HTTP server i otvara web pretraživač za pristup Loongcode-u preko web interfejsa. Postavite `LOONGCODE_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `loongcode`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba slušati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | Omogući mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### acp

Pokrenite ACP (Agent Client Protocol) server.

```bash
loongcode acp
```

Ova naredba pokreće ACP server koji komunicira preko stdin/stdout koristeći nd-JSON.

#### Opcije

| Opcija                                   | Opis                        |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Radni direktorij            |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem slušati   |

---

### uninstall

Deinstalirajte Loongcode i uklonite sve povezane datoteke.

```bash
loongcode uninstall
```

#### Opcije

| Opcija                                      | Kratko | Opis                                          |
| ------------------------------------------- | ------ | --------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`   | Sačuvajte konfiguracijske datoteke            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`   | Sačuvajte podatke i snimke sesije             |
| <nobr><code>{"--dry-run"}</code></nobr>     |        | Pokažite šta bi bilo uklonjeno bez uklanjanja |
| <nobr><code>{"--force"}</code></nobr>       | `-f`   | Preskoči upite za potvrdu                     |

---

### upgrade

Ažurira Loongcode na najnoviju verziju ili određenu verziju.

```bash
loongcode upgrade [target]
```

Za nadogradnju na najnoviju verziju.

```bash
loongcode upgrade
```

Za nadogradnju na određenu verziju.

```bash
loongcode upgrade v0.1.48
```

#### Opcije

| Opcija                                 | Kratko | Opis                                                    |
| -------------------------------------- | ------ | ------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`   | Korišteni način instalacije; curl, npm, pnpm, bun, brew |

---

## Globalne opcije

Loongcode CLI prihvata sljedeće globalne zastavice.

| Opcija                                     | Kratko | Opis                                      |
| ------------------------------------------ | ------ | ----------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`   | Prikaži pomoć                             |
| <nobr><code>{"--version"}</code></nobr>    | `-v`   | Ispiši broj verzije                       |
| <nobr><code>{"--print-logs"}</code></nobr> |        | Ispis logova u stderr                     |
| <nobr><code>{"--log-level"}</code></nobr>  |        | Nivo logovanja (DEBUG, INFO, WARN, ERROR) |

---

## Varijable okruženja

Loongcode se može konfigurirati pomoću varijabli okruženja.

| Varijabla                             | Tip     | Opis                                                               |
| ------------------------------------- | ------- | ------------------------------------------------------------------ |
| `LOONGCODE_AUTO_SHARE`                 | boolean | Automatski dijeli sesije                                           |
| `LOONGCODE_GIT_BASH_PATH`              | string  | Putanja do Git Bash izvršne datoteke na Windows-u                  |
| `LOONGCODE_CONFIG`                     | string  | Putanja do konfiguracijskog fajla                                  |
| `LOONGCODE_TUI_CONFIG`                 | string  | Putanja do TUI konfiguracijskog fajla                              |
| `LOONGCODE_CONFIG_DIR`                 | string  | Putanja do konfiguracijskog direktorija                            |
| `LOONGCODE_CONFIG_CONTENT`             | string  | Inline json konfiguracijski sadržaj                                |
| `LOONGCODE_DISABLE_AUTOUPDATE`         | boolean | Onemogući automatske provjere ažuriranja                           |
| `LOONGCODE_DISABLE_PRUNE`              | boolean | Onemogući brisanje (pruning) starih podataka                       |
| `LOONGCODE_DISABLE_TERMINAL_TITLE`     | boolean | Onemogući automatsko ažuriranje naslova terminala                  |
| `LOONGCODE_PERMISSION`                 | string  | Inline json konfiguracija dozvola                                  |
| `LOONGCODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Onemogući podrazumijevane dodatke (plugins)                        |
| `LOONGCODE_DISABLE_LSP_DOWNLOAD`       | boolean | Onemogući automatsko preuzimanje LSP servera                       |
| `LOONGCODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Omogući eksperimentalne modele                                     |
| `LOONGCODE_DISABLE_AUTOCOMPACT`        | boolean | Onemogući automatsko sažimanje konteksta                           |
| `LOONGCODE_DISABLE_CLAUDE_CODE`        | boolean | Onemogući čitanje iz `.claude` (prompt + vještine)                 |
| `LOONGCODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Onemogući čitanje `~/.claude/CLAUDE.md`                            |
| `LOONGCODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Onemogući učitavanje `.claude/skills`                              |
| `LOONGCODE_DISABLE_MODELS_FETCH`       | boolean | Onemogući dohvaćanje modela iz udaljenih izvora                    |
| `LOONGCODE_FAKE_VCS`                   | string  | Lažni VCS provajder za potrebe testiranja                          |
| `LOONGCODE_CLIENT`                     | string  | Identifikator klijenta (zadano na `cli`)                           |
| `LOONGCODE_ENABLE_EXA`                 | boolean | Omogući Exa alate za web pretraživanje                             |
| `LOONGCODE_SERVER_PASSWORD`            | string  | Omogući osnovnu autentifikaciju za `serve`/`web`                   |
| `LOONGCODE_SERVER_USERNAME`            | string  | Poništi osnovno korisničko ime autentifikacije (zadano `loongcode`) |
| `LOONGCODE_MODELS_URL`                 | string  | Prilagođeni URL za dohvaćanje konfiguracije modela                 |

---

### Eksperimentalno

Ove varijable okruženja omogućavaju eksperimentalne karakteristike koje se mogu promijeniti ili ukloniti.

| Varijabla                                       | Tip     | Opis                                                    |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `LOONGCODE_EXPERIMENTAL`                         | boolean | Omogući eksperimentalne funkcije pod zbirnom zastavicom |
| `LOONGCODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Omogući otkrivanje ikona                                |
| `LOONGCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Onemogući kopiranje pri odabiru u TUI                   |
| `LOONGCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Zadano vremensko ograničenje za bash naredbe u ms       |
| `LOONGCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksimalni izlazni tokeni za LLM odgovore               |
| `LOONGCODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Omogući praćenje datoteka za cijeli direktorij          |
| `LOONGCODE_EXPERIMENTAL_OXFMT`                   | boolean | Omogući oxfmt formatter                                 |
| `LOONGCODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Omogući eksperimentalni LSP alat                        |
| `LOONGCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Onemogući praćenje datoteka                             |
| `LOONGCODE_EXPERIMENTAL_EXA`                     | boolean | Omogući eksperimentalne Exa funkcije                    |
| `LOONGCODE_EXPERIMENTAL_LSP_TY`                  | boolean | Omogući TY LSP za python datoteke                       |
| `LOONGCODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Omogući Plan mod                                        |
| `LOONGCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Omogući pozadinske zadatke subagenata                   |
| `LOONGCODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Omogući eksperimentalni sistem događaja                 |
| `LOONGCODE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Omogući nativnu putanju LLM zahtjeva                    |
| `LOONGCODE_EXPERIMENTAL_PARALLEL`                | boolean | Omogući paralelno izvršavanje web pretrage              |
| `LOONGCODE_EXPERIMENTAL_SCOUT`                   | boolean | Omogući Scout subagenta                                 |
| `LOONGCODE_EXPERIMENTAL_WORKSPACES`              | boolean | Omogući podršku za radne prostore                       |
